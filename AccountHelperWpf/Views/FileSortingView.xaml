<UserControl x:Class="AccountHelperWpf.Views.FileSortingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:AccountHelperWpf.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:FileSortingViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" CanContentScroll="True" PreviewMouseWheel="UIElement_OnPreviewMouseWheel">
            <ItemsControl ItemsSource="{Binding OperationsGroups, Mode=OneTime}" Width="Auto">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBox Text="{Binding Name, Mode=OneTime}" />
                            <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Operations}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding Operation.TransactionDateTime}" Header="Transaction Date Time" />
                                    <DataGridTextColumn Binding="{Binding Operation.OperationName}" Header="Description" />
                                    <DataGridTextColumn Binding="{Binding Operation.Amount}" Header="Amount" />
                                    <DataGridTextColumn Binding="{Binding Operation.Currency}" Header="Currency" />
                                    <DataGridTextColumn Binding="{Binding Operation.Fee}" Header="Fee" />
                                    <DataGridTextColumn Binding="{Binding Operation.AccountAmount}" Header="Account Amount" />
                                    <DataGridTemplateColumn Header="Categories" CanUserSort="True" SortMemberPath="Category">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding Categories, Mode=OneTime}"
                                                          SelectedItem="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <TextBlock Margin="5,30,5,5" Grid.Row="1"  Text="{Binding Summary}"></TextBlock>
    </Grid>
</UserControl>
